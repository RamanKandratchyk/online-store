import { CartPromocodeState } from '../types/interfaces';

const promocodesData: CartPromocodeState[] = [
  { id: 'rs', name: 'Rolling Scopes School', discount: 10 },
  { id: 'epm', name: 'EPAM Systems', discount: 10 },
];

export default promocodesData;
